import { JsonPipe, DatePipe } from '@angular/common'; import { ChangeDetectionStrategy, Component, DestroyRef, inject, OnInit, signal } from '@angular/core'; import { takeUntilDestroyed } from '@angular/core/rxjs-interop'; import { ActivatedRoute, RouterLink } from '@angular/router'; import { AdminRevoqraService } from '../../core/revoqra/admin-revoqra.service'; import { BillingEventDetail } from '../../core/revoqra/admin-revoqra.models'; import { AdminHeaderComponent } from '../../shared/admin-header/admin-header';
@Component({selector:'app-revoqra-event-detail',imports:[DatePipe,RouterLink,AdminHeaderComponent],templateUrl:'./revoqra-event-detail.html',styleUrl:'./revoqra-event-detail.scss',changeDetection:ChangeDetectionStrategy.OnPush})
export class RevoqraEventDetailComponent implements OnInit { private readonly service=inject(AdminRevoqraService); private readonly route=inject(ActivatedRoute); private readonly destroyRef=inject(DestroyRef); readonly event=signal<BillingEventDetail|null>(null); readonly payload=signal(''); ngOnInit(){this.service.getEvent(this.route.snapshot.paramMap.get('eventId')!).pipe(takeUntilDestroyed(this.destroyRef)).subscribe(e=>{this.event.set(e);try{this.payload.set(JSON.stringify(JSON.parse(e.payloadJson),null,2));}catch{this.payload.set(e.payloadJson);}});} }
